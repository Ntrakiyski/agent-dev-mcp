# Chrome MCP Server - Environment Variables
# Copy this file to .env and fill in your values (if needed)

# Python Configuration
# Ensures real-time logging output (already set in docker-compose.yml)
PYTHONUNBUFFERED=1

# MCP Server Configuration (optional)
# Uncomment and set if you need custom configuration
# MCP_SERVER_PORT=8000
# MCP_SERVER_HOST=0.0.0.0

# Note: Most deployments don't need custom environment variables
# The defaults in docker-compose.yml and server.py work out-of-the-box

